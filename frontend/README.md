## Getting Started

1. Clone the repository: `git clone https://github.com/username/repository.git`
2. Install the dependencies: `npm install`
3. Start the development server: `npm run dev`
4. Open the app in your browser: `http://localhost:3000`

## API Endpoints

* `GET /posts`: Retrieve a list of all blog posts
* `GET /posts/:id`: Retrieve a single blog post by ID
* `POST /posts`: Create a new blog post
* `PUT /posts/:id`: Update an existing blog post
* `DELETE /posts/:id`: Delete a blog post
* `GET /designs`: Retrieve a list of all mobile app designs
* `GET /designs/:id`: Retrieve a single mobile app design by ID
* `POST /designs`: Create a new mobile app design
* `PUT /designs/:id`: Update an existing mobile app design
* `DELETE /designs/:id`: Delete a mobile app design

## Contributing

1. Fork the repository: `git fork https://github.com/username/repository.git`
2. Create a new branch: `git branch feature/new-feature`
3. Make your changes and commit them: `git commit -m 'Added new feature'`
4. Push your changes: `git push origin feature/new-feature`
5. Create a pull request: `https://github.com/username/repository/pulls`

## License

This project is licensed under the MIT License. See the LICENSE file for details.